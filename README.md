# DjangoAPIWithVueBoilerplate
## Prepare
## To run
### For server
`pip3 install -r requirements.txt`
`python manage.py migrate`
`python manage.py runserver`

### For frontend
`cd frontend`
`npm install`
`npm run serve`